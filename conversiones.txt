int32_t x=84225;
int8_t y;

y=x;

        movb    x, %al
        movb    %al, y
//-------------------------------------------------------------------//
int32_t x=84225;
int16_t y;

y=x;

        movw    x, %ax
        movw    %ax, y
//-------------------------------------------------------------------//
int16_t x=25732;
int8_t y;

y=x;

        movb    x, %al
        movb    %al, y

//-------------------------------------------------------------------//
int8_t     x=25;
int16_t   y;
y=x;
		movsbw	   x, %ax 
           	movw     %ax, y
//-------------------------------------------------------------------//
int8_t     x=25;
int32_t   y;
y=x
		movsbl	   x, %eax 
           	movl     %eax, y
//-------------------------------------------------------------------------------//

Int16_t     x=25734;
int32_t    y;
y=x
	    movswl   x, %eax 
        movl     %eax, y
//-------------------------------------------------------------------------------//
Int8_t     x=25;
float       y;
y=x
        movsbl      x, %eax
        cvtsi2ssl   %eax, %xmm0
        movss       %xmm0, y

//------------------------------------------------------------------------------------//
Int16_t     x=25732;
float       y;
y=x
        movswl      x, %eax
        cvtsi2ssl   %eax, %xmm0
        movss       %xmm0, y

//------------------------------------------------------------------------------------//
Int32_t     x=45624;
float         y;
y=x
        movl        x, %eax
        cvtsi2ssl   %eax, %xmm0
        movss       %xmm0, y

//---------------------------------------------------------------------------------------//
float       x=25.46;
int8_t     y;
y=x
        movss   	   x, %xmm0
        cvttss2sil    %xmm0, %eax
        movb    	   %al, y
//---------------------------------------------------------------------------------------//
float         x=25.46;
int16_t     y;
y=x
        movss   	    x, %xmm0
        cvttss2sil     %xmm0, %eax
        movw           %ax, y
//---------------------------------------------------------------------------------------//
float         x=25.46;
int32_t     y;
y=x
        movss          x, %xmm0
        cvttss2sil     %xmm0, %eax
        movl           %eax, y
//---------------------------------------------------------------------------------------//

Notes:
cbtw: AX := sign-extend of AL.
cwtl: EAX := sign-extend of AX

      
